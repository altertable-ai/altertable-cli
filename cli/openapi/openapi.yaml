# AUTO-GENERATED by `bundle exec rake openapi:generate` - DO NOT EDIT.
# Edit config/openapi/rest_v1.yaml.erb or the controllers' REST::Input/REST::Output structs instead.
openapi: 3.1.0
info:
  title: Altertable Management API
  version: v1
  description: |
    Management API for programmatic access to Altertable resources.
    Authenticate with a management API key using Bearer token authentication.

servers:
  - url: /rest/v1
    description: REST API v1

security:
  - bearerAuth: []

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      description: "Management API key token (e.g. `atm_...`)"

  schemas:
    Error:
      type: object
      required: [error]
      properties:
        error:
          type: object
          required: [code, message]
          properties:
            code:
              type: string
              enum: [unauthorized, forbidden, not_found, bad_request, unprocessable_content]
            message:
              type: string
            details:
              type: array
              items:
                type: string

    Bucket:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        slug:
          type: string
        region:
          type: string
        endpoint:
          type: string
        provider:
          type: string
          enum:
            - s3
            - r2
            - gcs
            - custom
        built_in:
          type: boolean
        environment_id:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
      required:
        - id
        - name
        - slug
        - provider
        - built_in
        - environment_id
        - created_at
        - updated_at
    BucketResponse:
      type: object
      properties:
        bucket:
          "$ref": "#/components/schemas/Bucket"
      required:
        - bucket
    BucketsListResponse:
      type: object
      properties:
        buckets:
          type: array
          items:
            "$ref": "#/components/schemas/Bucket"
      required:
        - buckets
    Connection:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        slug:
          type: string
        engine:
          type: string
          enum:
            - postgres
            - bigquery
            - redshift
            - snowflake
            - mariadb
            - mysql
            - supabase
            - buckettables
            - icebergtables
            - r2catalog
            - s3tables
            - glue
            - duckdb
        read_only:
          type: boolean
        description:
          type: string
        labels:
          type: array
          items:
            "$ref": "#/components/schemas/Label"
        catalog:
          type: string
        environment_id:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
      required:
        - id
        - name
        - slug
        - engine
        - read_only
        - labels
        - catalog
        - environment_id
        - created_at
        - updated_at
    ConnectionBigQueryConfig:
      type: object
      properties:
        dataset:
          type: string
        project_id_override:
          type: string
    ConnectionBucketTablesConfig:
      type: object
      properties:
        bucket_id:
          type: string
        file_format:
          type: string
          enum:
            - parquet
            - csv
            - json
        assume_immutable:
          type: boolean
        tables: {}
      required:
        - tables
    ConnectionDuckDBConfig:
      type: object
      properties:
        bucket_id:
          type: string
        path:
          type: string
    ConnectionGlueConfig:
      type: object
      properties:
        warehouse:
          type: string
        default_region:
          type: string
        role_arn:
          type: string
    ConnectionIcebergTablesConfig:
      type: object
      properties:
        bucket_id:
          type: string
        tables: {}
      required:
        - tables
    ConnectionMysqlConfig:
      type: object
      properties:
        host:
          type: string
        port:
          type: integer
        database:
          type: string
        username:
          type: string
        password:
          type: string
        schema:
          type: string
        ssh_tunnel:
          "$ref": "#/components/schemas/ConnectionSshTunnel"
    ConnectionPostgresConfig:
      type: object
      properties:
        host:
          type: string
        port:
          type: integer
        database:
          type: string
        username:
          type: string
        password:
          type: string
        schema:
          type: string
        ssh_tunnel:
          "$ref": "#/components/schemas/ConnectionSshTunnel"
        sslmode:
          type: string
    ConnectionR2CatalogConfig:
      type: object
      properties:
        warehouse:
          type: string
        endpoint:
          type: string
        token:
          type: string
    ConnectionResponse:
      type: object
      properties:
        connection:
          "$ref": "#/components/schemas/Connection"
      required:
        - connection
    ConnectionS3TablesConfig:
      type: object
      properties:
        warehouse:
          type: string
        default_region:
          type: string
        aws_access_key_id:
          type: string
        aws_secret_access_key:
          type: string
    ConnectionSnowflakeConfig:
      type: object
      properties:
        account_url:
          type: string
        warehouse:
          type: string
        username:
          type: string
        password:
          type: string
        database:
          type: string
    ConnectionSshTunnel:
      type: object
      properties:
        bastion_host:
          type: string
        bastion_port:
          type: integer
        bastion_username:
          type: string
    ConnectionStandardConfig:
      type: object
      properties:
        host:
          type: string
        port:
          type: integer
        database:
          type: string
        username:
          type: string
        password:
          type: string
        schema:
          type: string
        ssh_tunnel:
          "$ref": "#/components/schemas/ConnectionSshTunnel"
    ConnectionsListResponse:
      type: object
      properties:
        connections:
          type: array
          items:
            "$ref": "#/components/schemas/Connection"
      required:
        - connections
    CreateBucketRequest:
      type: object
      properties:
        name:
          type: string
        access_key_id:
          type: string
        secret_access_key:
          type: string
        region:
          type: string
        endpoint:
          type: string
      required:
        - name
        - access_key_id
        - secret_access_key
    CreateConnectionRequest:
      type: object
      properties:
        name:
          type: string
        engine:
          type: string
          enum:
            - postgres
            - bigquery
            - redshift
            - snowflake
            - mariadb
            - mysql
            - supabase
            - buckettables
            - icebergtables
            - r2catalog
            - s3tables
            - glue
            - duckdb
        read_only:
          type: boolean
        label_ids:
          type: array
          items:
            type: string
        description:
          type: string
        standard_config:
          "$ref": "#/components/schemas/ConnectionStandardConfig"
        mysql_config:
          "$ref": "#/components/schemas/ConnectionMysqlConfig"
        postgres_config:
          "$ref": "#/components/schemas/ConnectionPostgresConfig"
        bigquery_config:
          "$ref": "#/components/schemas/ConnectionBigQueryConfig"
        snowflake_config:
          "$ref": "#/components/schemas/ConnectionSnowflakeConfig"
        bucket_tables_config:
          "$ref": "#/components/schemas/ConnectionBucketTablesConfig"
        iceberg_tables_config:
          "$ref": "#/components/schemas/ConnectionIcebergTablesConfig"
        duckdb_config:
          "$ref": "#/components/schemas/ConnectionDuckDBConfig"
        r2_catalog_config:
          "$ref": "#/components/schemas/ConnectionR2CatalogConfig"
        s3_tables_config:
          "$ref": "#/components/schemas/ConnectionS3TablesConfig"
        glue_config:
          "$ref": "#/components/schemas/ConnectionGlueConfig"
      required:
        - name
        - engine
    CreateCredentialRequest:
      type: object
      properties:
        label:
          type: string
        expires_at:
          type: string
          format: date-time
    CreateCredentialResponse:
      type: object
      properties:
        credential:
          "$ref": "#/components/schemas/Credential"
        password:
          type: string
      required:
        - credential
        - password
    CreateDatabaseRequest:
      type: object
      properties:
        name:
          type: string
        bucket_id:
          type: string
        read_only:
          type: boolean
        label_ids:
          type: array
          items:
            type: string
        snapshot_retention_days:
          type: integer
        description:
          type: string
      required:
        - name
    CreateEnvironmentRequest:
      type: object
      properties:
        name:
          type: string
        cloud_provider:
          type: string
          enum:
            - hetzner
            - aws
        cloud_provider_hetzner_region:
          type: string
          enum:
            - fsn1
        cloud_provider_aws_region:
          type: string
          enum:
            - eu-west-1
      required:
        - name
        - cloud_provider
    CreateServiceAccountRequest:
      type: object
      properties:
        label:
          type: string
      required:
        - label
    Credential:
      type: object
      properties:
        id:
          type: string
        label:
          type: string
        username:
          type: string
        default:
          type: boolean
        active:
          type: boolean
        environment_id:
          type: string
        created_at:
          type: string
          format: date-time
        expires_at:
          type: string
          format: date-time
        revoked_at:
          type: string
          format: date-time
        last_rotated_at:
          type: string
          format: date-time
      required:
        - id
        - label
        - username
        - default
        - active
        - environment_id
        - created_at
    CredentialResponse:
      type: object
      properties:
        credential:
          "$ref": "#/components/schemas/Credential"
      required:
        - credential
    Database:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        slug:
          type: string
        read_only:
          type: boolean
        built_in:
          type: boolean
        description:
          type: string
        labels:
          type: array
          items:
            "$ref": "#/components/schemas/Label"
        catalog:
          type: string
        bucket_id:
          type: string
        snapshot_retention_days:
          type: integer
        environment_id:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
      required:
        - id
        - name
        - slug
        - read_only
        - built_in
        - labels
        - catalog
        - bucket_id
        - environment_id
        - created_at
        - updated_at
    DatabaseResponse:
      type: object
      properties:
        database:
          "$ref": "#/components/schemas/Database"
      required:
        - database
    DatabasesListResponse:
      type: object
      properties:
        databases:
          type: array
          items:
            "$ref": "#/components/schemas/Database"
      required:
        - databases
    Environment:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        slug:
          type: string
        cloud_provider:
          type: string
          enum:
            - hetzner
            - aws
        cloud_provider_region:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
      required:
        - id
        - name
        - slug
        - cloud_provider
        - cloud_provider_region
        - created_at
        - updated_at
    EnvironmentResponse:
      type: object
      properties:
        environment:
          "$ref": "#/components/schemas/Environment"
      required:
        - environment
    Label:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        color:
          type: string
      required:
        - id
        - name
        - color
    Organization:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        slug:
          type: string
      required:
        - id
        - name
        - slug
    Principal:
      type: object
      properties:
        id:
          type: string
        type:
          type: string
          enum:
            - User
            - ServiceAccount
        name:
          type: string
        email:
          type: string
        slug:
          type: string
      required:
        - id
        - type
        - name
    RoleAssignment:
      type: object
      properties:
        role:
          type: string
        resource_kind:
          type: string
        resource_id:
          type: string
      required:
        - role
        - resource_kind
        - resource_id
    RoleAssignmentsResponse:
      type: object
      properties:
        role_assignments:
          type: array
          items:
            "$ref": "#/components/schemas/RoleAssignment"
      required:
        - role_assignments
    RoleInput:
      type: object
      properties:
        role:
          type: string
          enum:
            - catalog:reader
            - catalog:writer
            - environment:member
            - environment:reader
            - environment:writer
            - organization:member
            - organization:reader
            - organization:writer
            - organization:admin
        resource_kind:
          type: string
          enum:
            - organization
            - environment
            - catalog
        resource_id:
          type: string
      required:
        - role
        - resource_id
    ServiceAccount:
      type: object
      properties:
        id:
          type: string
        label:
          type: string
        slug:
          type: string
      required:
        - id
        - label
        - slug
    ServiceAccountResponse:
      type: object
      properties:
        service_account:
          "$ref": "#/components/schemas/ServiceAccount"
      required:
        - service_account
    UpdateBucketRequest:
      type: object
      properties:
        name:
          type: string
        access_key_id:
          type: string
        secret_access_key:
          type: string
        region:
          type: string
        endpoint:
          type: string
    UpdateConnectionRequest:
      type: object
      properties:
        name:
          type: string
        read_only:
          type: boolean
        label_ids:
          type: array
          items:
            type: string
        description:
          type: string
        standard_config:
          "$ref": "#/components/schemas/ConnectionStandardConfig"
        mysql_config:
          "$ref": "#/components/schemas/ConnectionMysqlConfig"
        postgres_config:
          "$ref": "#/components/schemas/ConnectionPostgresConfig"
        bigquery_config:
          "$ref": "#/components/schemas/ConnectionBigQueryConfig"
        snowflake_config:
          "$ref": "#/components/schemas/ConnectionSnowflakeConfig"
        bucket_tables_config:
          "$ref": "#/components/schemas/ConnectionBucketTablesConfig"
        iceberg_tables_config:
          "$ref": "#/components/schemas/ConnectionIcebergTablesConfig"
        duckdb_config:
          "$ref": "#/components/schemas/ConnectionDuckDBConfig"
        r2_catalog_config:
          "$ref": "#/components/schemas/ConnectionR2CatalogConfig"
        s3_tables_config:
          "$ref": "#/components/schemas/ConnectionS3TablesConfig"
        glue_config:
          "$ref": "#/components/schemas/ConnectionGlueConfig"
    UpdateDatabaseRequest:
      type: object
      properties:
        name:
          type: string
        read_only:
          type: boolean
        label_ids:
          type: array
          items:
            type: string
        snapshot_retention_days:
          type: integer
        description:
          type: string
    UpdateRoleAssignmentsRequest:
      type: object
      properties:
        roles:
          type: array
          items:
            "$ref": "#/components/schemas/RoleInput"
      required:
        - roles
    UpdateServiceAccountRequest:
      type: object
      properties:
        label:
          type: string
      required:
        - label
    User:
      type: object
      properties:
        id:
          type: string
        email:
          type: string
        name:
          type: string
      required:
        - id
        - email
        - name
    UserResponse:
      type: object
      properties:
        user:
          "$ref": "#/components/schemas/User"
      required:
        - user
    WhoamiResponse:
      type: object
      properties:
        principal:
          "$ref": "#/components/schemas/Principal"
        organization:
          "$ref": "#/components/schemas/Organization"
        authentication_scope:
          type: string
        environment_slug:
          type: string
      required:
        - principal
        - organization
        - authentication_scope

  responses:
    Unauthorized:
      description: Missing or invalid authentication token
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    Forbidden:
      description: Insufficient permissions
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"

paths:
  /whoami:
    get:
      operationId: whoami
      summary: Identify the authenticated principal
      description: Returns the principal (user or service account) and organization associated with the API key.
      tags: [Authentication]
      responses:
        "200":
          description: Authenticated principal and organization
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WhoamiResponse"
              examples:
                user:
                  summary: User principal
                  value:
                    principal:
                      id: "019d7223-796e-7917-8d7e-ca256daa7979"
                      type: User
                      name: Jane Doe
                      email: jane@example.com
                    organization:
                      id: "019d7223-796e-7917-8d7e-ca256daa7980"
                      name: Acme Corp
                      slug: acme-corp
                service_account:
                  summary: ServiceAccount principal
                  value:
                    principal:
                      id: "019d7223-796e-7917-8d7e-ca256daa7981"
                      type: ServiceAccount
                      name: CI Deploy
                      slug: ci-deploy
                    organization:
                      id: "019d7223-796e-7917-8d7e-ca256daa7980"
                      name: Acme Corp
                      slug: acme-corp
        "401":
          $ref: "#/components/responses/Unauthorized"

  /environments/{id}:
    get:
      operationId: getEnvironment
      summary: Get an environment by ID or slug
      tags: [Environments]
      parameters:
        - name: id
          in: path
          required: true
          description: Environment UUID or slug
          schema:
            type: string
            examples:
              - "019d7223-796e-7917-8d7e-ca256daa7979"
              - "production"
      responses:
        "200":
          description: Environment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvironmentResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      operationId: deleteEnvironment
      summary: Delete an environment
      tags: [Environments]
      parameters:
        - name: id
          in: path
          required: true
          description: Environment UUID or slug
          schema:
            type: string
            examples:
              - "019d7223-796e-7917-8d7e-ca256daa7979"
              - "production"
      responses:
        "204":
          description: Environment deleted
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /environments:
    post:
      operationId: createEnvironment
      summary: Create an environment
      tags: [Environments]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateEnvironmentRequest"
      responses:
        "201":
          description: Environment created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvironmentResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          description: Validation failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /service_accounts:
    post:
      operationId: createServiceAccount
      summary: Create a service account
      tags: [Service Accounts]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateServiceAccountRequest"
      responses:
        "201":
          description: Service account created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceAccountResponse"
        "400":
          description: Missing required parameter
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /service_accounts/{id}:
    get:
      operationId: getServiceAccount
      summary: Get a service account by ID
      tags: [Service Accounts]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Service account
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ServiceAccountResponse" }
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: updateServiceAccount
      summary: Update a service account
      tags: [Service Accounts]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateServiceAccountRequest" }
      responses:
        "200":
          description: Service account updated
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ServiceAccountResponse" }
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          description: Validation failed
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
    delete:
      operationId: deleteServiceAccount
      summary: Delete a service account
      tags: [Service Accounts]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: Service account deleted
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          description: Service account still has active credentials
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /environments/{environment_id}/buckets:
    get:
      operationId: listBuckets
      summary: List buckets in an environment
      tags: [Buckets]
      parameters:
        - name: environment_id
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Buckets
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BucketsListResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: createBucket
      summary: Connect a bucket
      tags: [Buckets]
      parameters:
        - name: environment_id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateBucketRequest" }
      responses:
        "201":
          description: Bucket created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BucketResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422":
          description: Validation failed
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /environments/{environment_id}/buckets/{id}:
    get:
      operationId: getBucket
      summary: Get a bucket by ID or slug
      tags: [Buckets]
      parameters:
        - name: environment_id
          in: path
          required: true
          schema: { type: string }
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Bucket
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BucketResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateBucket
      summary: Update a bucket
      tags: [Buckets]
      parameters:
        - name: environment_id
          in: path
          required: true
          schema: { type: string }
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateBucketRequest" }
      responses:
        "200":
          description: Bucket updated
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BucketResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422":
          description: Validation failed
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
    delete:
      operationId: deleteBucket
      summary: Delete a bucket
      tags: [Buckets]
      parameters:
        - name: environment_id
          in: path
          required: true
          schema: { type: string }
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        "204": { description: Bucket deleted }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422":
          description: Built-in buckets cannot be deleted
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /environments/{environment_id}/connections:
    get:
      operationId: listConnections
      summary: List connections in an environment
      tags: [Connections]
      parameters:
        - name: environment_id
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Connections
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ConnectionsListResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: createConnection
      summary: Create a connection
      tags: [Connections]
      parameters:
        - name: environment_id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateConnectionRequest" }
      responses:
        "201":
          description: Connection created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ConnectionResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422":
          description: Validation failed
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /environments/{environment_id}/connections/{id}:
    get:
      operationId: getConnection
      summary: Get a connection by ID or slug
      tags: [Connections]
      parameters:
        - name: environment_id
          in: path
          required: true
          schema: { type: string }
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Connection
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ConnectionResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateConnection
      summary: Update a connection
      tags: [Connections]
      parameters:
        - name: environment_id
          in: path
          required: true
          schema: { type: string }
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateConnectionRequest" }
      responses:
        "200":
          description: Connection updated
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ConnectionResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422":
          description: Validation failed
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
    delete:
      operationId: deleteConnection
      summary: Delete a connection
      tags: [Connections]
      parameters:
        - name: environment_id
          in: path
          required: true
          schema: { type: string }
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        "204": { description: Connection deleted }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /environments/{environment_id}/databases:
    get:
      operationId: listDatabases
      summary: List databases in an environment
      tags: [Databases]
      parameters:
        - name: environment_id
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Databases
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DatabasesListResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: createDatabase
      summary: Create a database
      tags: [Databases]
      parameters:
        - name: environment_id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateDatabaseRequest" }
      responses:
        "201":
          description: Database created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DatabaseResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422":
          description: Validation failed
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /environments/{environment_id}/databases/{id}:
    get:
      operationId: getDatabase
      summary: Get a database by ID or slug
      tags: [Databases]
      parameters:
        - name: environment_id
          in: path
          required: true
          schema: { type: string }
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Database
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DatabaseResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      operationId: updateDatabase
      summary: Update a database
      tags: [Databases]
      parameters:
        - name: environment_id
          in: path
          required: true
          schema: { type: string }
        - name: id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateDatabaseRequest" }
      responses:
        "200":
          description: Database updated
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DatabaseResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422":
          description: Validation failed
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
    delete:
      operationId: deleteDatabase
      summary: Delete a database
      tags: [Databases]
      parameters:
        - name: environment_id
          in: path
          required: true
          schema: { type: string }
        - name: id
          in: path
          required: true
          schema: { type: string }
      responses:
        "204": { description: Database deleted }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /users/lookup:
    get:
      operationId: lookupUser
      summary: Look up a user in the organization by email
      tags: [Users]
      parameters:
        - name: email
          in: query
          required: true
          description: Email address of the user to look up
          schema: { type: string }
      responses:
        "200":
          description: User
          content:
            application/json:
              schema: { $ref: "#/components/schemas/UserResponse" }
        "400":
          description: Missing required parameter
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /users/{id}:
    get:
      operationId: getUser
      summary: Get a user in the organization by ID
      tags: [Users]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        "200":
          description: User
          content:
            application/json:
              schema: { $ref: "#/components/schemas/UserResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /users/{user_id}/role_assignments:
    get:
      operationId: getUserRoleAssignments
      summary: List a user's direct role assignments
      tags: [Role Assignments]
      parameters:
        - { name: user_id, in: path, required: true, schema: { type: string } }
      responses:
        "200":
          description: Role assignments
          content:
            application/json:
              schema: { $ref: "#/components/schemas/RoleAssignmentsResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    put:
      operationId: setUserRoleAssignments
      summary: Replace a user's role assignments
      tags: [Role Assignments]
      parameters:
        - { name: user_id, in: path, required: true, schema: { type: string } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateRoleAssignmentsRequest" }
      responses:
        "200":
          description: Role assignments replaced
          content:
            application/json:
              schema: { $ref: "#/components/schemas/RoleAssignmentsResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422":
          description: Validation failed
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /service_accounts/{service_account_id}/role_assignments:
    get:
      operationId: getServiceAccountRoleAssignments
      summary: List a service account's direct role assignments
      tags: [Role Assignments]
      parameters:
        - { name: service_account_id, in: path, required: true, schema: { type: string } }
      responses:
        "200":
          description: Role assignments
          content:
            application/json:
              schema: { $ref: "#/components/schemas/RoleAssignmentsResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    put:
      operationId: setServiceAccountRoleAssignments
      summary: Replace a service account's role assignments
      tags: [Role Assignments]
      parameters:
        - { name: service_account_id, in: path, required: true, schema: { type: string } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateRoleAssignmentsRequest" }
      responses:
        "200":
          description: Role assignments replaced
          content:
            application/json:
              schema: { $ref: "#/components/schemas/RoleAssignmentsResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422":
          description: Validation failed
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /users/{user_id}/environments/{environment_id}/credentials:
    post:
      operationId: createUserCredential
      summary: Create a credential for a user (returns the password once)
      tags: [Credentials]
      parameters:
        - { name: user_id, in: path, required: true, schema: { type: string } }
        - { name: environment_id, in: path, required: true, schema: { type: string } }
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateCredentialRequest" }
      responses:
        "201":
          description: Credential created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CreateCredentialResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422":
          description: Validation failed
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /users/{user_id}/environments/{environment_id}/credentials/{id}:
    get:
      operationId: getUserCredential
      summary: Get a user's credential (no secret)
      tags: [Credentials]
      parameters:
        - { name: user_id, in: path, required: true, schema: { type: string } }
        - { name: environment_id, in: path, required: true, schema: { type: string } }
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        "200":
          description: Credential
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CredentialResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      operationId: revokeUserCredential
      summary: Revoke a user's credential
      tags: [Credentials]
      parameters:
        - { name: user_id, in: path, required: true, schema: { type: string } }
        - { name: environment_id, in: path, required: true, schema: { type: string } }
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        "204": { description: Credential revoked }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /service_accounts/{service_account_id}/environments/{environment_id}/credentials:
    post:
      operationId: createServiceAccountCredential
      summary: Create a credential for a service account (returns the password once)
      tags: [Credentials]
      parameters:
        - { name: service_account_id, in: path, required: true, schema: { type: string } }
        - { name: environment_id, in: path, required: true, schema: { type: string } }
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateCredentialRequest" }
      responses:
        "201":
          description: Credential created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CreateCredentialResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422":
          description: Validation failed
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /service_accounts/{service_account_id}/environments/{environment_id}/credentials/{id}:
    get:
      operationId: getServiceAccountCredential
      summary: Get a service account's credential (no secret)
      tags: [Credentials]
      parameters:
        - { name: service_account_id, in: path, required: true, schema: { type: string } }
        - { name: environment_id, in: path, required: true, schema: { type: string } }
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        "200":
          description: Credential
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CredentialResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      operationId: revokeServiceAccountCredential
      summary: Revoke a service account's credential
      tags: [Credentials]
      parameters:
        - { name: service_account_id, in: path, required: true, schema: { type: string } }
        - { name: environment_id, in: path, required: true, schema: { type: string } }
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        "204": { description: Credential revoked }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
